/**
 This is for testing the legacy interface to InfSOCSOl.
*/

!: matlab_eval "example_a" should output "" when
  addpath = matlab_call "addpath" 0;
  fullfile = matlab_call "fullfile" 1;
  pwd      = matlab_call "pwd" 1 ();

  addpath $ fullfile(pwd, "tests/example_a");  
end;
