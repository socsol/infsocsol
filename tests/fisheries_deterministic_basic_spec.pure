
matlab_addpath $ __dir__ + "/fisheries/deterministic/basic";

let state_lb = {60.0, 0.1};
let state_ub = {600.0, 1.0};
                                                     
//let conf
   iss_conf (state_lb, state_ub,
              "ControlLB", -0.01,
              "ControlUB", 0.01,
              "DiscountRate", 0.1,
              "UserConstraintFunctionFile", "fisheries_deterministic_basic_constraints");

//let ocm, u_optimal, value, flags
   iss_solve ("fisheries_deterministic_basic_delta",
               "fisheries_deterministic_basic_cost",
               state_lb, state_ub,
               conf);

!: "fisheries" => ["deterministic" => ["basic" => [ocm should be in matrix,
                                                   u_optimal should be in number]]];